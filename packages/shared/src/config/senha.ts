/** Regras da senha compartilhadas entre servidor e telas. */

/** Dias que uma senha provisória continua válida depois de gerada. */
export const DIAS_VALIDADE_PROVISORIA = 7;

/** Mínimo de caracteres da senha definitiva (igual no login, cadastro e troca). */
export const TAMANHO_MINIMO_SENHA = 6;
